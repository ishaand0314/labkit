# Optional. token-cost runs fine with no keys (OpenAI counts are always exact,
# others are honest estimates). Set these to upgrade Anthropic and Google to
# exact counts via their free count-tokens endpoints. Neither key is required.
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
